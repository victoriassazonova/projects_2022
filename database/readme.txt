Before running change in config.json in DB_DSN your information for connection.

Service that allows the user:
Learing - user enters training set and cv parameters. Server trains model - Linear regression with L2 reg and CV for parameters.
User can get trained model and its ID in the db and the results.
Prediction - user enters data set and id model. Server makes predictions for given data with model (model id from user).

